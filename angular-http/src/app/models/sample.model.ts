export class SampleModel {
  constructor(
    // These two is basic value in kintone tables
    // Customize your own value for the model
    public $revision?: string,
    public id?: number,
  ) {}
}
